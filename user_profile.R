# user_profile.R 

# In case the `renv` package does not set up the environment, run this script. 
# The following list include the packages used *directly* in this project.
# There will be more packages to be installed due to dependencies.
install.packages(c("tidyverse", "readxl", "here", "knitr"))
